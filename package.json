{
  "name": "foundertxt",
  "version": "1.0.0",
  "private": true,
  "description": "FounderTxt — Tweets that sound like you, not ChatGPT",
  "scripts": {
    "deploy": "vercel --prod",
    "dev": "vercel dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vitest": "^4.1.10"
  }
}
