{
  "name": "foundertxt",
  "version": "1.0.0",
  "private": true,
  "description": "FounderTxt — Tweets that sound like you, not ChatGPT",
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=foundertxt --branch=main",
    "dev": "wrangler pages dev .",
    "secret": "wrangler pages secret put DEEPSEEK_API_KEY --project-name=foundertxt"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
