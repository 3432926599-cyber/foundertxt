{
  "name": "foundertxt",
  "version": "1.0.0",
  "private": true,
  "description": "FounderTxt — Tweets that sound like you, not ChatGPT",
  "scripts": {
    "deploy": "netlify deploy --prod",
    "dev": "netlify dev",
    "deploy:old": "wrangler pages deploy . --project-name=foundertxt --branch=main",
    "secret": "netlify env:set DEEPSEEK_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
